# Taskboard Ogilvy

Minimal, mobile-first task tracker using YAML as source of truth.

## Run

```bash
npm install
npm run dev
```

Set env:

```bash
TASKS_PASSCODE=your-passcode
```

## Data

Update `data/tasks.yaml` and redeploy (or push to trigger deploy).
